import { useState, useEffect } from 'react'
import API_KEY from './keys'

const CONTEXT_KEY = "903c72868fc944769"

const useGoogleSearch = () => {
    const [data, setData] = useState(null);

    useEffect(() => {

    }, [term])

}

export default useGoogleSearch